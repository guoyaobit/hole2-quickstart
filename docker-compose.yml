version: "3.8"

services:
  hole-app:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./:/app
    environment:
      - PYTHONUNBUFFERED=1
    # By default start Jupyter Lab so the notebook UI is available
    command: jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root
