version: "3.8"

services:
  hole-app:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./:/app
    environment:
      - PYTHONUNBUFFERED=1
    # By default start Jupyter so the notebook UI is available
    command: jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser --allow-root
